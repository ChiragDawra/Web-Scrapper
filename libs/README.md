# libs

Shared code imported by every service, owned by no single service.
Placeholder. Created by Sprint 0 Task 0.1 (structure only, no implementation).

Mirrors `ZIP_13_ENGINEERING_CONTRACTS/`: `event_bus/schema/` holds one JSON Schema
per event type (`EVENT_SCHEMAS.md`), `canonical_models/` mirrors `CANONICAL_MODELS.md`,
`enums/` mirrors `ENUMS.md`, `error_codes/` mirrors `ERROR_CODES.md`.
