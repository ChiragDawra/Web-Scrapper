"""Per-marketplace connector tests.

A package, not a bare directory: every marketplace names its test module
`connector_test.py`, and pytest imports same-named modules as one unless each
directory is importable in its own right.
"""
